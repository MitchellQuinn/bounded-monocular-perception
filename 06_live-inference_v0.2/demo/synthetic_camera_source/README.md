# Synthetic Camera Demo Source

This directory contains a very small copied subset of the
`05_inference-v0.4-ts/input/def90_synth_v023-validation-shuffled/images`
validation image corpus for local GUI smoke testing.

The files are ordinary PNG copies, not symlinks. Add only a few images here so
the synthetic camera starts quickly.
